package com.facebook.imagepipeline.producers;

import com.facebook.imagepipeline.common.ResizeOptions;

public interface az<T> extends al<T> {
    boolean canProvideImageForSize(ResizeOptions resizeOptions);
}
