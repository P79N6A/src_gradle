package me.drakeet.multitype;

import android.support.annotation.CheckResult;
import android.support.annotation.NonNull;

public interface h<T> {
    @CheckResult
    @NonNull
    g<T> a(@NonNull c<T, ?>... cVarArr);
}
